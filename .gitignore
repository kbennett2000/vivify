# --- Third-party IP: NEVER commit ---
# Microsoft Agent character files
*.acs
*.acf
*.aca
*.acd
# Speech engine installers / binaries
spchapi.exe
tv_enua*.exe
*.sapi
# Belt-and-suspenders: never commit any Windows binary or the vendor dir
*.exe
services/voice-server/vendor/
# Wine prefix used by the voice server
services/voice-server/.wine/
services/voice-server/prefix/
# Extracted Microsoft assets (bitmaps/sounds pulled out of .acs during dev)
**/fixtures/**/*.bmp
**/fixtures/**/*.wav
**/fixtures/raw/

# --- Standard ---
node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo
.DS_Store
.env
.env.local

# --- Personal local config (not for a public repo) ---
.claude/settings.local.json

# --- Cycle 1 oracle (GPL DoubleAgent source + the VM dumper package + its output) ---
# Never commit: GPL code, the dumper glue that links it, or extracted MS assets.
oracle/
tools/oracle/
# Decoded images / composited frames emitted by our spike-dump (extracted MS assets)
packages/acs/scripts/out/
# Cycle 3 local visual-confirmation harness (not the committed app — that's Cycle 4 MASH)
packages/core/dev/
