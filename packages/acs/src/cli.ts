// acs2bundle CLI — converts a raw .acs into a web-ready vivify bundle.
//
// Stub only. Real conversion lands in Cycle 2.

export function main(argv: string[]): void {
  void argv;
  console.error('acs2bundle: not implemented yet (stub). See docs/cycles/cycle-2.');
}

main(process.argv.slice(2));
