// @vivify/voice-server — the authentic-voice backend. A Dockerized
// Wine + SAPI4 + L&H TruVoice service that returns { audio, mouthTimeline }.
//
// Stub only. The HTTP service + Wine/SAPI4 bridge land in Cycle 5. No real
// logic here yet. The vendor/ binaries and Wine prefix/ are gitignored and
// must never be committed.

export const name = '@vivify/voice-server';
